# E2E house rules

The house-rules marker is `{{HOUSE_MARKER}}`.

When a task requests a JSON result artifact, include that exact value as `houseMarker`. Keep all work inside the fixture repository. Never use external services.
