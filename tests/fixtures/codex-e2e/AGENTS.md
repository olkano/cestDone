# Fixture rules

Read and follow these rules for every task in this repository.

- The repository rule marker is `{{RULE_MARKER}}`.
- When a task requests a JSON result artifact, include that exact value as `ruleMarker`.
- Do not access the network, send messages, push, or modify anything outside this fixture repository.
- Run `node --test` after changing `sum.mjs`.
